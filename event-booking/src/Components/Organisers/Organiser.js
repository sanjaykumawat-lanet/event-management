import Dashboard from "./Dashboard";

const Organiser = (props) => {
  return (
    <div>
      {/* <Header /> */}
      <Dashboard {...props}/>
    </div>
  );
};

export default Organiser;
